import 'package:flutter/material.dart';

/// Simple data holder for a single sidebar navigation entry.
class NavItemData {
  final IconData icon;
  final String label;
  const NavItemData(this.icon, this.label);
}
